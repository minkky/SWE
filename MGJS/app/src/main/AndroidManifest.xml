<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mgjs.mgjs">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity"
            android:theme="@android:style/Theme.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ValidateUserActivity"
            android:theme="@android:style/Theme.NoTitleBar"/>
        <activity android:name=".ChangeUserActivity"
            android:theme="@android:style/Theme.NoTitleBar"/>
        <activity android:name=".ScheduleActivity"
            android:theme="@android:style/Theme.NoTitleBar"/>
        <activity android:name=".Add_ScheduleActivity"
            android:theme="@android:style/Theme.NoTitleBar"/>
        <activity android:name=".Detail_ScheduleActivity"
            android:theme="@android:style/Theme.NoTitleBar"/>
        <activity android:name=".PhoneBookActivity"
            android:theme="@android:style/Theme.NoTitleBar"/>
        <activity android:name=".Add_PhoneNumberActivity"
            android:theme="@android:style/Theme.NoTitleBar"/>
        <activity android:name=".Detail_PhoneNumberActivity"
            android:theme="@android:style/Theme.NoTitleBar"/>
        <activity android:name=".NoteActivity"
            android:theme="@android:style/Theme.NoTitleBar"/>
        <activity android:name=".Add_NoteActivity"
            android:theme="@android:style/Theme.NoTitleBar"/>
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>